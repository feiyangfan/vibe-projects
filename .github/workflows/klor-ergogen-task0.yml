name: KLOR Task 0 - historical feasibility

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Task 0 is complete; see klor-trackball/ergogen/TASK0_RESULT.md"
